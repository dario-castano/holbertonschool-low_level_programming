#include <stdio.h>
/**
 * main - entry point
 * @argc: number of args
 * @argv: array of params
 * Return: 0
 */
int main(int argc, char **argv)
{
	if (argc == 1)
	{
		puts(argv[0]);
	}

	return (0);
}
